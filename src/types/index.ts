export interface ICharacters {
  id: number;
  name: string;
  imageUrl: string;
}

export interface ICharacter {
  id: number;
  films: string;
  name: string;
  imageUrl: string;
  sourceUrl: string;
}

export interface IParams {
  id: number;
}
